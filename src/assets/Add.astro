---
interface Props {
  width?: number;
  height?: number;
  color?: string;
  class?: string;
}

const { width = 24, height = 24, class: className = "", color="currentColof" } = Astro.props;
---

<svg xmlns="http://www.w3.org/2000/svg" width={width} height={height} viewBox="0 0 32 32"><path fill={color} stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 25V7m-9 9h18"/></svg>